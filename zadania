zadanie1



#include<iostream>

using namespace std;
int main() 
{
	double dystans, czas;
	cout << "podaj dystans w km:";
	cin >> dystans;

	cout << "podaj czas w godzinach:";
	cin >> czas;

	if (czas <= 0)
	{ 
		cout << "czas musi byc wiekszy od zera!";
		
	}
	
	double predkosc = dystans / czas;
	cout << "srednia predkosc:" << predkosc << "Km/h";
	






}
zadanie 2
#include<iostream>
#include<math.h>
using namespace std;
int main() 
{
	int sekundy, minuty, godziny;
	   cout << "podaj sekundy:";
	cin >> sekundy;
	if (sekundy > 3600) {
		cout << "tyle sekund to:" << sekundy / 3600;
		cout << "godziny" << endl;
	 }
	minuty = sekundy % 3600;
	if(minuty >= 60){
		cout << "tyle sekund to:" << minuty / 60;
		cout << "minuty" << endl;
	}
	godziny = minuty % 60;
	if (godziny < 60) {
		cout << "reszta sekund:" << godziny;
	}
zadanie 3
#include<iostream>
#include<math.h>
using namespace std;
int main() 
{
	double moc, czas, koszt, zmienna;
	float cenaZ;
	cout << "ile mocy pobiera to urzadzenie?:";
	cin >> moc;
	cout << "ilosc godzin pracy urzadzenia:";
	cin >> czas;
	cout << "ile uzylo urzadzenie podczas miesiaca:" << (moc * czas) / 1000;
	cout << "KWh" << endl;
	koszt = 0.62;
	zmienna = (moc * czas) / 1000;
	cenaZ = koszt * zmienna;
	cout << "koszt miesieczny wychodzi" << cenaZ;
	cout << "zl";
zadanie 4
#include<iostream>
#include<math.h>
using namespace std;
int main() 
{
	double zarobek, prog1, prog2, cz1, cz2, cz3;
	prog1 = 0.12;
	prog2 = 0.32;
	cout << "ile zarabiasz miesięcznie?:";
	cin >> zarobek;
	if (zarobek <= 30000) {
		cout << "podatek wynosi:" << zarobek * prog1;
    }
	else if (zarobek > 30000) {
		cz1 = zarobek - 30000;
		cz2 = cz1 * prog2;
		cz3 = zarobek * prog1;
		cout << "podatek wynosi:" << cz2 + cz3;
	}








}










}
zadanie 5
#include<iostream>

using namespace std;
 int main() 
 {
	 int n;
	 cout << "Podaj liczbe: ";
	 cin >> n; 

	 bool pierwsza = true;

	 if (n < 2) {
		 pierwsza = false;
	 }
	 else {
		 for (int i = 2; i * i <= n; i++) {
			 if (n % i == 0) {
				 pierwsza = false;
				 break;
			 }
		 }
	 }
	 if (pierwsza) cout << n << "jest liczba pierwsza. " << endl;
	 else cout << "liczba nie jest pierwsza" << endl;
	 
